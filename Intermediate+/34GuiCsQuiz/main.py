# Stuff we need to make this app is 
    # Getting hold of api 
    # Next we need to revise Tkinter and 
    
from quiz_ques import GetQuestions
import tkinter as tk

# Creating a window
screen = tk.Tk()

# Setting up title 
screen.title("Tech Quiz")
# Setting window size
screen.geometry('400x400')
#Setting bg color
screen.configure(background="light green")

# What things we need 
# Two labels
    # One for maintaining score
    # Other for Showing the question
# Two buttons
    # True and,
    # False

screen.mainloop()
